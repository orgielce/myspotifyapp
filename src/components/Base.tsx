import React from "react";
import styled from "styled-components";

const Element = styled.div`
  display: flex;
`;

export const BaseCreateComponent = () => {
  return <Element>Content</Element>;
};
